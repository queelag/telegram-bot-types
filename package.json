{
  "author": "Dario Sechi",
  "devDependencies": {
    "@aracna/core": "^1.1.41",
    "@types/cheerio": "^0.22.31",
    "@vitest/coverage-v8": "^0.34.2",
    "@vitest/ui": "^0.34.2",
    "cheerio": "^1.0.0-rc.12",
    "typescript": "^5.1.6",
    "vitest": "^0.34.2"
  },
  "license": "MIT",
  "main": "index.js",
  "name": "@aracna/telegram-bot-types",
  "repository": "https://github.com/aracna/telegram-bot-types",
  "scripts": {
    "test": "yarn vitest run",
    "build": "rm -rf dist && rm -rf declarations && yarn tsc && node index.js && : > dist/index.js",
    "tarball": "yarn version && cp LICENSE package.json README.md dist/. && cd dist && yarn pack",
    "deploy": "yarn test && yarn build && yarn tarball && yarn publish dist/*.tgz && git push"
  },
  "version": "1.2.4"
}
